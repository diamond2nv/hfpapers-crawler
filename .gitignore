# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# ── Data / Output ──
data/
pdfs/
mds/
md_extracts/
logs/

# ── Database ──
*.db
*.db-wal
*.db-shm

# ── External Dependencies ──
venv/
.venv/
.uv/
uv.lock
.env

# ── Security ──
# .env and .env.* contain API keys and secrets, never commit
.env*
!.env.template

# Generated media (Manim, etc.)
media/

# ── System ──
.DS_Store
*.swp
*.swo

# ── IDE ──
.vscode/
.idea/

# ── Scrapy ──
*.pyc
.cache/

# ── Test Reports ──
htmlcov/
.coverage
.pytest_cache/
# Downloaded paper PDFs (large, generated)
raw_pdfs/

# ── arXiv source bundles (fetched intermediates) ──
sources/

# ── Hermes Agent (internal) ──
.hermes/

# ── CodeGraph index (generated) ──
.codegraph/
scripts/researcher-audit/people.yaml

# ── Local config overlay (real names / ORCIDs / local paths — NEVER public) ──
config.local.yaml
scripts/cron-repos.local.json

# ── Profiles (sensitive — real user interest must NEVER be public) ──
.hfpclawer/
*_profile.yaml
profile.yaml
